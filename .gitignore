# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Test + coverage output. Regenerated by `bun run test:coverage`; Jenkins
# archives it as build artifacts, so it must never be committed.
coverage/
reports/
